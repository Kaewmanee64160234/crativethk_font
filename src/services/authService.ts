// src/services/AuthService.ts

import axios from 'axios';

const API_URL = 'http://localhost:3000/auth/'; // Adjust to your backend URL

class AuthService {
  // const userProfile = null;
  // async loginWithGoogle() {
  //   try {
  //     // After successful login, fetch the user profile from your backend
  //     // For example, using fetch API to call your NestJS backend
  //     const response = await fetch('/api/auth/profile');
  //     this.userProfile = await response.json();
  //   } catch (error) {
  //     console.error('Fetch profile error:', error);
  //   }
  // }

  // Method to handle other auth-related functionalities
}

export default new AuthService();
